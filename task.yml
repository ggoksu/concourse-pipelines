---
platform: linux

image_resource:
  type: registry-image
  source: {repository: ggoksu/docker-image-resource}
  privileged: true

params:
  REPOSITORY: harbor.sandbox.demo1.pocs.pcfs.io/applications/sample_java_app
  BUILDER: cloudfoundry/cnb:bionic
  TAG: latest
  APP_DIR: source-app

inputs:
- name: source-app

outputs:
- name: image

run: {path: build}
