---
platform: linux

image_resource:
  type: registry-image
  source: {repository: ggoksu/docker-image-resource}

params:
  REPOSITORY: ((repository))
  BUILDER: ((builder))
  TAG: latest
  APP_DIR: source-app

inputs:
- name: source-app

outputs:
- name: image

run: {path: build}
